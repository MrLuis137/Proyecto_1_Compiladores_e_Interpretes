let
  recursive
    func doble(var i : Integer) : Integer ~
      i+3 ! sintácticamente bien, contextualmente mal
    and
    proc triplicar(var i : Integer) ~
      i := i + doble(i)
    end
    and
    proc duplicar(var i : Integer) ~
      i := doble(i)
    end
  end
in
  nothing 
end